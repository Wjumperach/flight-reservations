import { Routes } from '@angular/router';

import { ReservationsContainerComponent } from './containers/reservations-container/reservations-container.component';

export const routes: Routes = [
    {
        path: '',
        component: ReservationsContainerComponent
    }
];
